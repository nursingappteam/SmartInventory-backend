const argon2 = require("argon2");

let createUser = (username, password) => {
  let hash;
  try{
    
  } catch(err) {
    
  }
}

let verifyUser = () => {
  
}

module.export = {createUser, verifyUser};